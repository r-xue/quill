# Read the Docs configuration
# https://docs.readthedocs.io/en/stable/config-file/v2.html

version: 2

build:
  os: ubuntu-24.04
  tools:
    python: '3.12'

  commands:
    - pip install -e .[docs]
    - zensical build
    - cp -a site/ $READTHEDOCS_OUTPUT/html/
